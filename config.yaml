expirement_name: "detection_baseline"
seed: 42

data:
  dataset_path:
  train_split:
  val_split:
  test_split:
  image_size:
  batch_size:
  num_workers:

model:
  architecture: 
  pretrained:

training:
  epochs:
  learning_rate:
  optimizer:
  loss_function:
  early_stopping:
  patience:

output:
  model_save_path:
  logs_path:
